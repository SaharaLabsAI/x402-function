apiVersion: v1
kind: Service
metadata:
  name: x402-function-backend
  namespace: x402-function
  labels:
    app: x402-function-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: x402-function-backend
